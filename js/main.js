$(document).ready(function() {
  console.log("Hai.");

  // updateNetwork();
  // loadTimeSeires();
  //
  // TODO need to load activity intervals as well.

  // clientHandler(d)

  // var nodeNames = d.network.union.vertices
  // // constructs the suggestion engine
  // var nodeNames = new Bloodhound({
  //   datumTokenizer: Bloodhound.tokenizers.obj.whitespace('value'),
  //   queryTokenizer: Bloodhound.tokenizers.whitespace,
  //   local: $.map(nodeNames, function(state) { return { value: state }; })
  // });

  // // kicks off the loading/processing of `local` and `prefetch`
  // nodeNames.initialize();
  // $('#nodeSelector .typeahead').typeahead({
  //   hint: true,
  //   highlight: true,
  //   minLength: 1
  // },
  // {
  //   name: 'nodeNames',
  //   displayKey: 'value',
  //   source: nodeNames.ttAdapter()
  // }).on('typeahead:autocompleted', function($e, datum){
  //   zoomToNode(datum.value);
  // }).on('typeahead:selected', function($e, datum){
  //   zoomToNode(datum.value);
  // });
})
